#include <stdio.h>
//int indica que a função retorna para um valor inteiro
//main indica o nome da função principal, que é o ponto de entrada do programa
//() indica que a função não recebe argumentos
int main() {
    printf("Hello, world!");
    return 0;
}

//printf serve para exibir informações no console