#include <stdio.h>

int main () {
    int A, B, PROD;
    printf("insira o valor de A:");
    scanf("%d", &A);
    printf("Insira o valor de B:");
    scanf("%d", &B);
    PROD = A*B;
    printf("PROD = %d\n", PROD);

return 0;
}